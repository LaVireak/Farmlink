# Farmlink Backend

This repository contains the backend API for the Farmlink project, built with NestJS and PostgreSQL. It provides authentication, farmer management, product ordering, notifications, reviews, rewards, and support content.

3. Run the app locally

```bash
npm run start:dev
```

## 🐳 Docker

If you use Docker, start the service with:

```bash
docker compose up --build
```

